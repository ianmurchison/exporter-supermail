{[ const frame = context.frame /]}

{* Only render if present *}
{[ if frame.previewUrl ]}
    <img src="{{ frame.previewUrl }}" />
{[/]}

{[ if frame.description ]}
    <p class="align-center subtle">{{ frame.description }}</p>    
{[/]}
